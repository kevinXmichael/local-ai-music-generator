# Core (always)
numpy>=1.24
soundfile>=0.12
librosa>=0.10
typer>=0.12
rich>=13.0
pyyaml>=6.0
platformdirs>=4.0

# Optional source separation + torch audio (recommended for real covers)
# demucs>=4.0
# torch>=2.1
# torchaudio>=2.1

# Dev / CI
pytest>=8.0
pytest-cov>=5.0
ruff>=0.6
